import styles from './styles.module.scss'
const Loader = () => {
    return (
        <div className={styles.loader}>
            if page is not loading - allow location access and reload page
        </div>

    )
}

export {Loader}